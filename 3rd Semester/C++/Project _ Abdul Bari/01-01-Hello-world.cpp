#include <iostream> // library header files for cpp

// we may also use #include <iostream.h>

int main()

{

    std::cout << "Hello world!";

    // cout - console output(monitor) // and cin - console input(keyboard) // '<<' insertion operator

    // cout<<"String"; '<<' give the feeling as if we are inserting inide 'cout'

    // double colon ' :: ' is called as scope resolution

    return 0; // since main is of int type
}